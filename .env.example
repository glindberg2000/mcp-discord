# Discord Configuration for Testing
# Note: The multi-bot API doesn't require these at startup
# Tokens are passed via HTTP requests instead

DISCORD_TOKEN=your-primary-discord-bot-token-here
DISCORD_TOKEN_2=your-secondary-discord-bot-token-here
DEFAULT_SERVER_ID=your-discord-server-id-here

# Optional: Database connection for memory (if using Postgres container)
POSTGRES_URL=postgresql://superagent:superagent@postgres:5432/superagent