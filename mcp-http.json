{
  "mcpServers": {
    "discord-http": {
      "command": "curl",
      "args": [
        "-X", "POST",
        "-H", "Content-Type: application/json",
        "-H", "Authorization: Bot YOUR_TOKEN_HERE",
        "http://localhost:9090"
      ],
      "transport": {
        "type": "http",
        "url": "http://localhost:9090"
      },
      "description": "Discord operations via HTTP API supporting multiple bot tokens"
    }
  }
}